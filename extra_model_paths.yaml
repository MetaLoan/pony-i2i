# ComfyUI 自动扫描这个文件
# 所有模型都在 /workspace/models/ (网络卷, 永久持久化)
# 不需要软链接!

pony_workspace:
    base_path: /workspace/models
    checkpoints: checkpoints
    loras: loras
    ipadapter: ipadapter
    insightface: insightface
    instantid: instantid
    controlnet: controlnet
